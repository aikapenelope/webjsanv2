---
import { MARCAS } from '../data/site';

// Carga eagerly todos los SVGs para inyectarlos en línea y permitir transiciones CSS nativas
const marcasSvg = import.meta.glob('../assets/marcas/*.svg', {
  query: '?raw',
  import: 'default',
  eager: true,
}) as Record<string, string>;

const getSvg = (marca: string) => marcasSvg[`../assets/marcas/${marca}.svg`] ?? '';
---

<section id="marcas" class="marcas-sec">
  <div class="wrap">
    <div class="sec-head">
      <span class="kicker">Compatibilidad multimarca</span>
      <h2 class="sec-title">Atendemos las marcas líderes del mercado</h2>
      <p class="sec-lead">Dominamos las transmisiones automáticas, CVT y secuenciales de fabricantes americanos, asiáticos y europeos.</p>
    </div>
  </div>

  <div class="marquee-wrap" aria-label="Carrusel de marcas atendidas">
    <div class="marquee-track">
      {[0, 1].map(() =>
        MARCAS.map((m) => (
          <div class="mq-item" title={`Especialistas en ${m}`}>
            <span class="mq-icon" set:html={getSvg(m)} />
            <span class="mq-label">{m}</span>
          </div>
        ))
      )}
    </div>
  </div>
</section>

<style>
  .marcas-sec {
    padding: 4.2rem 0 0;
    background: #ffffff;
    border-top: 1px solid var(--sky-line);
  }

  .marquee-wrap {
    overflow: hidden;
    padding: 2rem 0;
    border-top: 1px solid var(--sky-line);
    border-bottom: 1px solid var(--sky-line);
    background: var(--sky);
    mask-image: linear-gradient(90deg, transparent, #000 6%, #000 94%, transparent);
    -webkit-mask-image: linear-gradient(90deg, transparent, #000 6%, #000 94%, transparent);
  }

  .marquee-track {
    display: flex;
    align-items: center;
    gap: 3.4rem;
    width: max-content;
    animation: mq 38s linear infinite;
  }

  .marquee-wrap:hover .marquee-track {
    animation-play-state: paused;
  }

  .mq-item {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    color: #94a1be;
    text-decoration: none;
    flex: none;
    transition: color 0.15s ease;
  }

  .mq-item:hover {
    color: var(--blue);
  }

  .mq-icon {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .mq-icon :global(svg) {
    width: 36px;
    height: 36px;
    fill: currentColor;
  }

  .mq-label {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 1.25rem;
    letter-spacing: 0.07em;
    text-transform: uppercase;
    color: var(--navy);
  }

  .mq-item:hover .mq-label {
    color: var(--blue);
  }

  @media (max-width: 768px) {
    .marquee-track {
      gap: 2.2rem;
    }
    .mq-icon :global(svg) {
      width: 28px;
      height: 28px;
    }
    .mq-label {
      font-size: 1.1rem;
    }
  }
</style>
